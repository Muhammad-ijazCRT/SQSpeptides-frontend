/** Placeholder vial-style product shots; swap URLs when assets are ready. */
export function popularPepImage(seed: string): string {
  return `https://picsum.photos/seed/${encodeURIComponent(seed)}/640/800`;
}
